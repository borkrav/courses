#!/bin/csh

# Run the same program consecutive times. Should run properly each time.
# Requires quite a bit of memory
# Forces lots of tlb faults.
sys161 kernel "p newtests/vm-data2; p newtests/vm-data2; p newtests/vm-data2; p newtests/vm-data2; p newtests/vm-data2; p newtests/vm-data2; q"
